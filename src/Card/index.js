import React from 'react';
import PropTypes from 'prop-types';

const Card = (props) => {
  return(
    <div className="category-card">
      
    </div>
  );
}

Card.propTypes = {
  
}

export default Card;